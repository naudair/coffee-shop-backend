// db ruu handdag product tai hamaaraltai buh function bain

import { Product } from "../utils/types";
// import { useState } from "react";

export const getCoffees = async (): Promise<Product[]> => {
  // const coffees = await Product.find({})
  return [];
};
